package view;

import java.util.regex.Matcher;

public class GameMenu {
    public void run(){
        while (true){
            String command = ScanMatch.getScanner().nextLine();
            Matcher matcher;
        }
    }
}
